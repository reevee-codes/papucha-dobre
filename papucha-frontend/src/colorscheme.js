import { Appearance, StyleSheet, Text, View } from 'react-native';

export const colors = {
    background: "#fff",
    button: "#48F",
    // background: Appearance.getColorScheme() === "dark" ? "#222" : "#fff",
    text: "#111"
    // font: Appearance.getColorScheme() === "dark" ? "#ddd" : "#111"
};
